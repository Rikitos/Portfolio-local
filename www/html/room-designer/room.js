class Room {
  // x, y: top-left corner in world cm
  // width, height: room outer dimensions in cm
  constructor(x, y, width, height) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
  }
}
